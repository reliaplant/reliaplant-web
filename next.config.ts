import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Change the output directory to Firebase folder
  distDir: "firebase",

  // Keep space for future configurations
};

export default nextConfig;
