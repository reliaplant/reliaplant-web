# Pendientes — Reliaplant Web

## 30 de julio — Puntos a revisar página web

- [x] Términos de Uso: revisar colores / Colores de la web que coincidan más — encontré la causa raíz técnica (no era subjetivo): el sitio mezclaba dos paletas de gris distintas — clases tipo `gray70`/`gray90` (definidas en `tailwind.config.ts` con valores propios sin relación a Tailwind, ej. `#525252`) y clases nativas `gray-700`/`gray-900` de Tailwind (con tinte azulado, ej. `#374151`). El azul ya estaba unificado de una sesión anterior (`blue60` = `#2563eb` = mismo hex que `blue-600`), pero el gris nunca se alineó. Remapeé `gray10-100` y sus variantes `Hover` en `tailwind.config.ts` para que coincidan exactamente con la escala estándar de Tailwind (mismo patrón ya usado para blue) — verificado que `gray90` y `gray-900` ahora renderizan el mismo color exacto.
  - **Bug más grave encontrado de paso**: Términos de Uso, Privacidad y Cookies (y cualquier página sin fondo explícito) se veían **completamente negras** para cualquier visitante con modo oscuro activado en su sistema/navegador — el sitio nunca declaraba `color-scheme: light` ni tenía `bg-white` en el `<body>`, así que el navegador pintaba el canvas por defecto en negro en vez de blanco, dejando el texto oscuro casi invisible. Arreglado en `src/app/layout.tsx`: agregado `export const viewport: Viewport = { colorScheme: "light" }` y `bg-white` explícito en el body. Esto probablemente afectaba a un porcentaje importante de visitantes (cualquiera con dark mode del sistema activado) y nadie lo había reportado — no es algo que se note fácil sin herramientas.
- [x] Política de cookies — creada en `/legal/cookies`, enlazada desde el footer y desde la sección 15 de Política de Privacidad (que antes decía incorrectamente "no usamos cookies publicitarias ni de seguimiento", corregido). Cubre analítica (GA4/GTM), futuras cookies de Ads, y opción de exclusión para residentes de California (CCPA/CPRA), dado que el público es LatAm + EE.UU.
- [x] Minilogo / favicon — creado: la "R" de RELIAPLANT en Zen Dots (la tipografía real del logo, verificada bajando el archivo de fuente directo de Google Fonts) en blanco dentro de un círculo **negro**. Centrado por **centroide/baricentro real** (centro de masa ponderado por píxeles de tinta, medido programáticamente sobre el glifo renderizado — no por bounding box ni por métricas de fuente, que dejan la letra visiblemente descentrada por el espacio reservado a descendentes que la R no usa). Convergió con 0.24px de error tras 2 pasadas. Generado en varios tamaños (16/32/48/180/192/512) + `favicon.ico` multi-resolución construido a mano. Instalado en **ambos repos**. Verificado en vivo y con build de producción limpio en los dos.
- [x] Imágenes que no están en `/public` — investigado a fondo (hipótesis de Andrés sobre carga lenta/intermitente). Encontré 3 problemas reales:
  1. **Imagen rota (404) en `/consultoria/rca`**: `<img src="/arbol-logico.png">` nunca existió en `/public`. La oculté (comentario TODO) hasta que se consiga el diagrama correcto — sigue pendiente conseguir el archivo.
  2. **Sección completa "Casos de Estudio" invisible en `/consultoria`**: el componente `Industrias` se importaba pero nunca se colocaba en el JSX de la página, así que 5 casos de estudio reales (Oil & Gas Houston, hipercompresor petroquímico, plataformas offshore, etc.) nunca se mostraban. Además tenía imports rotos apuntando a una carpeta `src/assets` que no existe. Arreglé los imports (ahora apuntan a `/public/assets` correctamente) y agregué `<Industrias />` al final de la página. Verificado en vivo, sin errores de consola.
  3. **Imágenes de fondo/hero sobredimensionadas**: 31 archivos en `public/assets` pesaban hasta 4-7MB con resoluciones de cámara sin redimensionar (ej. `roi-cumplimiento-regulatorio.webp` a 6720×4480px, `engineeringRoom.webp` usado con `priority` como fondo decorativo al 30% de opacidad). Redimensionadas todas a un máximo de 2400px de ancho — **18.3MB ahorrados en total**, sin pérdida de calidad visible (verificado). Esto sí explica cargas lentas reales, especialmente la primera vez que el optimizador de Next.js procesa cada imagen.
- [x] Botón de "volver atrás" — era el botón circular flotante en la esquina superior de las páginas de módulo (`/modulos/rcm`, `/modulos/rca`, `/modulos/registro-activos`), posicionado en `right-0`. Movido a `left-0` en las tres páginas, verificado visualmente.
- [x] Google Tag Manager instalado en `reliaplant.com` (`GTM-PXC76M9X`) + GA4 conectado (`G-8ETKRG5XV5`, tag "Etiqueta de Google", publicado y verificado en vivo) + medición entre dominios configurada (reliaplant.com + app.reliaplant.com).
- [x] `app.reliaplant.com` — ya tenía Analytics propio vía Firebase SDK (`services/analytics/analytics.ts`, mismo `G-E6KR6QQJBT`), con eventos personalizados (login, logout, etc.). No necesitó cambios.
- [ ] **Verificar medición entre dominios de verdad funciona** — la app usa el SDK de Firebase Analytics, no `gtag.js` directo. No confirmado si lee el parámetro de continuidad de sesión que manda la web. Revisar en unos días en GA4 → Recorrido del usuario: alguien que pasa de la web a la app, ¿aparece como una sesión o como dos?
- [ ] Google Ads: falta configurar los tags de **conversión** dentro de GTM (eventos generales + conversión para campañas) — eso se hace desde la consola cuando arranquen las campañas.
- [ ] Enfocarse en landing pages y blogs

## Notas manuscritas (página web) — 2 de agosto

- [ ] Pricing, términos y condiciones, políticas *(ya cubierto arriba)*
- [x] Formularios / tracking — esto es lo que se hizo hoy (GTM + GA4 + tags de conversión)
- [ ] Cambiar colores *(ya cubierto arriba — "colores que coincidan más")*

*(Las notas de IH-08/IH-06/ISO-14224/$200/Onboarding de la foto son de un proyecto de consultoría con cliente aparte, no de reliaplant-web — no las incluyo aquí. Avísame si quieres que las guarde en otro archivo.)*

## Pendientes de sesiones anteriores

- [ ] **`BREVO_LIST_ID`** — los leads se sincronizan a Brevo pero no entran a ninguna lista todavía, así que ninguna automatización de correo dispara.
- [ ] **Automatización de email en Brevo** — crear la secuencia en sí (estrategia/copy la define Tomas).
- [x] **`OPENAI_API_KEY` real** — obsoleto: el generador de posts con IA se migró a Claude (`ANTHROPIC_API_KEY`, ver entrada del 2 de agosto abajo), ya no usa OpenAI.
- [x] **Desplegar reliaplant-app** — desplegado a `app.reliaplant.com` (hosting target `ramacombinada`). Verificado en el origen (Cloud Run) que los links de Términos/Privacidad ya apuntan a la web. La caché de CDN de algunas rutas tardó en propagarse pero el contenido servido es correcto.
- [ ] **Reglas de Firestore abiertas** — decisión: dejarlo pendiente por ahora, no tocar todavía (riesgo de romper producción si se hace mal). Investigación ya hecha para cuando se retome:
  - `firestore.rules` en `reliaplant-web` es el **único archivo de reglas que existe en todo el proyecto** — `reliaplant-app` nunca tuvo reglas propias, así que el catch-all `allow read, write: if true` afecta también al producto real, no solo a la web.
  - La app usa colecciones planas + campo `organizationId` para separar datos por cliente (85 usos de `where("organizationId", ...)` encontrados): `assets`, `rcas`, `rcm`, `sistemas`, `dtis`, `organizations`, `users`, `backups`, `upgrade_requests`.
  - **No usa Firebase custom claims** — no hay forma simple de saber a qué organización pertenece un usuario desde una regla sin hacer un `get()` extra a `users/{uid}` dentro de la regla misma.
  - Plan propuesto (no ejecutado): Fase 1 = asegurar solo las colecciones propias de la web (`form_contact`, `blogPosts`, `adminWeb`, `config`, y revisar qué son `mailQueue`/`pruebas`/`infoRequests`) — bajo riesgo. Fase 2 = diseñar reglas para las colecciones del producto y **probarlas en el emulador de Firebase antes de publicarlas** — nunca deployar reglas nuevas de las colecciones del producto directo a producción sin probar antes, por el riesgo de bloquear a clientes reales de su propia información.
- [ ] **Sección "Disponibilidad del servicio" (SLA) en Términos de Uso** — ya no menciona nombres de plan viejos, pero nunca se confirmó si el SLA del 99% y los tiempos de respuesta de soporte siguen siendo los reales/vigentes.
- [ ] **Precios en Términos de Uso de la app (`reliaplant-app`)** — el documento de Términos aún vive duplicado ahí (idéntico al de la web al día de hoy). Si se vuelve a editar el de la web, hay que recordar que la app tiene su propia copia — no quedó conectado por link como se hizo con Política de Privacidad/Términos en Configuración.
- [ ] **`upgrade_requests` (app) vs `form_contact` (web)** — ya unificado: los nuevos leads de upgrade se guardan en `form_contact` y sincronizan a Brevo. La colección vieja `upgrade_requests` y su panel propio (`UpgradeRequests.tsx`) se dejaron intactos a propósito (no se migraron datos históricos ni se retiró el panel).

## 2 de agosto — Homogeneidad visual del sitio (a partir de comparación app vs web)

- [x] **Componente `<Boton>` compartido** (`src/components/Boton.tsx`) — antes no existía ninguno; 27 usos de `buttonColor`/`textColor` con strings crudos (13 combinaciones distintas para "botón azul") migrados a 5 variantes fijas (`primary`, `outline`, `outline-light`, `white`, `dark`). Migrados también los botones standalone de Header, Footer, HeaderMovil, hero de homepage y tarjetas de módulo/precio — de paso corregidos varios que usaban `rounded` o sin radio en vez de `rounded-lg`.
- [x] Tabla de `/pricing` — quitados colores hex crudos inline, reemplazados por tokens Tailwind.
- [x] Tabla de `consultoria/rcm` — 96 instancias de tamaño de texto en `vw` (frágil en pantallas angostas) reemplazadas por tamaños fijos.
- [x] Fondos oscuros del sitio — el gradiente hero (`from-gray-900 via-[#1a1a2e] to-blue-900`) ya estaba bien convergido en 9 páginas. Encontré y arreglé un intruso real: `como-funciona/page.tsx` tenía un overlay que arrancaba en `#0f172a` (slate-900, no gray-900) — corregido a `gray-900`.
- [x] Fondo de `DemoRequestForm` (modal de "Solicita una cotización" en todo el sitio) — usaba `#001d6c`, un azul suelto sin relación a nada más. Cambiado a `#111827` (gray-900), el mismo navy que confirmamos coincide con el header de `reliaplant-app`.
- [x] **Bug de precios real (no cosmético)**: la sección de precios de la landing (`page.tsx`) tenía 4 planes hardcodeados con nombres y precios que ya no coincidían con `/pricing` (que sí lee de Firestore) — un visitante podía ver "$1,009/año, plan Equipo" en el home y "$2,976/año, plan Planta" al dar clic a la comparación completa. Conecté la sección del landing al mismo `config/planTemplates` de Firestore — ahora es imposible que se desalineen. De paso quité un toggle mensual/anual inventado (Firestore solo tiene precio anual) y una mención a un "Plan Empresa" que no existe como tal.
- [ ] Tokens Carbon (`blue60`, `gray90`, etc.) en `tailwind.config.ts` — ya alineados en hex con Tailwind, pero siguen siendo dos nombres para lo mismo. No los borré: tienen 500+ usos reales en el código, es un cambio grande. Queda como deuda técnica documentada, no como bug visual.
- [ ] Footer usa `bg-gray-950` (un paso más oscuro que el `gray-900` del resto del sitio) — dejado a propósito, parece intencional (footer más oscuro que el hero es común), pero no se confirmó con Tomas si debería ser exactamente el mismo tono.
- [x] **Bug real reportado por Tomas**: el logo "RELIAPLANT" del header mostraba primero una tipografía de fallback y luego "saltaba" a la correcta al cargar. Causa: Zen Dots se cargaba con un `@import url(fonts.googleapis.com/...)` en CSS normal en vez de con `next/font/google` (como sí hacía IBM Plex Sans) — eso agrega 2 saltos de red (CSS externo → luego el archivo woff2) antes de que el navegador tenga la fuente, y mientras tanto muestra el fallback (`font-display: swap`). Migrado a `next/font/google` igual que IBM Plex Sans: ahora se auto-hospeda desde el propio dominio (`/_next/static/media/*.woff2`), cero llamadas a Google Fonts, confirmado en Network. De paso eliminé una definición `.font-ZenDots` duplicada/muerta en globals.css que quedó de antes.

## 2 de agosto — Editor de blog y SEO técnico

- [x] **Bug grave**: crear un post nuevo con imagen de portada dejaba la imagen rota para siempre. `createBlogPost` devuelve el ID nuevo pero `handleSave` lo descartaba — `blogPost.id` nunca se actualizaba tras crear, así que la subida de la imagen (que esperaba a tener un ID real) nunca se disparaba. El resultado: un `blob:` URL (válido solo en esa pestaña, esa sesión) quedaba guardado permanentemente en Firestore como `coverImage`, y el archivo real nunca llegaba a Firebase Storage. Reescribí el flujo: `handleSave` ahora captura el ID real al crear, sube la imagen pendiente con ese ID, y actualiza el documento con la URL real antes de navegar fuera.
- [x] `handleSave` no tenía try/catch — si Firestore fallaba al guardar, el usuario no recibía ningún aviso. Agregado manejo de errores con toast.
- [x] La generación automática de slug no quitaba acentos (`Análisis` → `análisis-` en vez de `analisis-`). Nueva función compartida `generateSlug()` en `src/lib/utils/slug.ts`, usada tanto en el editor como en el fallback de guardado.
- [x] Sitemap (`sitemap.ts`) no incluía `/industrias` ni `/legal/cookies` (la que se creó ayer) — agregadas, verificado en el XML generado.
- [x] `blog/[slug]/page.tsx` traía TODOS los posts (publicados + borradores) solo para filtrar uno por slug — cambiado a `getPublishedBlogPosts()`, la query ya filtrada de Firestore.
- [x] El grid de `/blog` marcaba la imagen de portada Y la foto de autor de **cada tarjeta** como `priority`/`loading="eager"` — anulaba el lazy-loading para cualquier tarjeta fuera de la vista inicial. Ahora solo las primeras 3 tarjetas (above-the-fold) son priority; el resto carga lazy.
- [ ] **No se pudo probar el flujo completo en vivo** (crear post con imagen → verificar en Firestore/Storage) porque el panel admin requiere login real y no tengo credenciales. Verificado a nivel de código (tipos limpios, build limpio, trace lógico del flujo corregido) pero recomiendo que Tomas lo pruebe una vez desplegado.
- [ ] Blog sigue con **cero posts publicados** — la parte de contenido (escribir artículos reales) sigue pendiente, esto solo fue la parte de mecánica/SEO del editor.
- [x] **Generador de blog con IA reconectado** — estaba roto (usaba OpenAI con `OPENAI_API_KEY` placeholder). Migrado a Claude (`@anthropic-ai/sdk`), reusando la misma `ANTHROPIC_API_KEY` que ya funciona en reliaplant-app (decisión de Tomas: reusar en vez de key separada). `openai` quitado de package.json (ya no se usa). Ambas rutas (`/api/generate-post`, `/api/seo-suggest`) probadas en vivo contra la API real de Claude — funcionando, buena calidad de contenido en español. De paso encontré y arreglé que `claude-sonnet-5` ya no acepta el parámetro `temperature` (deprecado para este modelo) — no afecta a la app porque sus rutas activas nunca lo pasaban.

## 3 de agosto — CRM de leads (form-manager) y rediseño del blog

- [x] **Botón "+ Agregar lead"** en `/admin/form-manager` — modal para cargar leads manualmente (no vienen de un formulario del sitio), reusa `saveFormContact` así también sincroniza a Brevo.
- [x] **Vista de detalle del lead rediseñada** varias veces según feedback: sidebar angosto → split horizontal → **vista de pantalla completa** con tarjetas (Info / Tareas y seguimientos / Notas), que fue la que quedó.
- [x] **"Seguimiento" ahora es un tipo de tarea** — antes era un campo único `proximoSeguimiento` por lead; ahora es `LeadTarea` con `tipo: "tarea" | "seguimiento"`, permitiendo varios por lead. Migración automática al cargar: los leads con el campo viejo se convierten solos en una tarea tipo Seguimiento.
- [x] **Vista "Hoy"** (ahora la que abre por defecto) — lista unificada de tareas/seguimientos pendientes o vencidos de **todos** los leads, no solo lo agendado para un lead a la vez.
- [x] **Botones de contacto rápido** (📞 Llamar / 💬 WhatsApp / ✉️ Email) en la vista Hoy y en el detalle del lead — quedan como nota "pendiente de confirmar" (no se marca contacto real hasta confirmar con un detalle de qué se habló), porque abrir la app no significa que el mensaje/llamada se haya concretado.
- [x] **Editar y eliminar** notas y tareas/seguimientos (antes solo se podían agregar).
- [x] Renombrado "Formularios" → "CRM" en el menú y dashboard del admin.
- [x] **Rediseño del blog post** (`BlogPostContent.tsx`): columna más ancha (768px → 896px), índice lateral auto-generado con scroll-spy, tiempo de lectura estimado, callouts con color para bloques ✅/❌.
  - **Bug real encontrado de paso**: `tailwind.config.ts` no incluía `src/lib/**` en el `content` scan, así que Tailwind purgaba las clases `.callout*` (vivían en un archivo `.ts` fuera del scan). Agregado al config — afecta a cualquier clase de Tailwind escrita en `src/lib/`, no solo esta.
- [ ] **Los 11 posts del blog tienen `published: false` en Firestore** — confirmado consultando Firestore directamente. Por eso el post usado para probar el rediseño da 404 en producción (`reliaplant.com/blog/pasos-para-hacer-un-analisis-causa-raiz`). No se tocó ese campo desde el código — hay que decidir si se publica.
